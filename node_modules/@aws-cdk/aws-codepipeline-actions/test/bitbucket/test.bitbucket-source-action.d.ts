import { Test } from 'nodeunit';
declare const _default: {
    'BitBucket source Action': {
        'produces the correct configuration when added to a pipeline'(test: Test): void;
    };
};
export = _default;
